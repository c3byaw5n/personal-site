services:
  app:
    container_name: personal-site-app
    build:
      context: .
      dockerfile: docker/app/Dockerfile
      target: prod
    networks:
      - personal_site
    restart: always
    deploy:
      resources:
        limits:
          cpus: '0.50'
          memory: 512M
    logging:
      driver: 'json-file'
      options:
        max-size: '10m'
        max-file: '3'

networks:
  personal_site:
    external: true
